# Web frontend + video_engine runtime dependencies.
# (Not the atlas ML stack — this is only what webapp/server.py + video_engine need.)
fastapi==0.115.6
uvicorn[standard]==0.34.0
edge-tts==7.2.8
requests>=2.31,<3
pillow==12.2.0
